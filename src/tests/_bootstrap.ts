// import * as mongoose from 'mongoose';

// (mongoose as any).Promise = global.Promise;

// before(() => {
//   return mongoose.connect('mongodb://localhost:27017/tests');
// });

// after(() => {
//   return mongoose.connection.db.dropDatabase()
//     .then(() => mongoose.disconnect());
// });
